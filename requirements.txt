# Python version: 3.12.x (recommended)
# Note: Python 3.14+ may have compatibility issues with some packages

# Core dependencies
requests==2.32.5
pandas==2.3.3
numpy==2.3.3
streamlit==1.50.0

# Environment & Configuration
python-dotenv==1.0.1

# FastAPI & API Server (optional - for local proxy)
fastapi==0.115.6
uvicorn[standard]==0.34.2
httpx==0.28.1
pydantic==2.10.6
